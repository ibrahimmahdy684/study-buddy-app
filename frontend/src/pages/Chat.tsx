/**
 * Chat route alias for /messages/:conversationId.
 * The Messages page handles both the conversation list and the active chat
 * pane (split view), driven by the optional :conversationId param.
 */
import Messages from './Messages'
export default Messages
